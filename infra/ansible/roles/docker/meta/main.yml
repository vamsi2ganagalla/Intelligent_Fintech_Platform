---
galaxy_info:
  author: vamsi2ganagalla
  description: Install Docker CE from upstream apt repo with production-grade daemon config
  company: CSE 816 academic project
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags:
    - docker
    - container

dependencies:
  - role: common
