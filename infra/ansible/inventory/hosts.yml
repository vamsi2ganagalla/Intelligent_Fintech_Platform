---
# Inventory for the Intelligent FinTech Platform
# Day 5 — Ansible Infrastructure as Code

all:
  children:
    # Local development environment (Vamsi's laptop)
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3

    # Vagrant VM target (Phase 5.9 — proves disaster recovery)
    # Uncomment when ready to test on clean Ubuntu VM
    # vagrant:
    #   hosts:
    #     fintech-vm:
    #       ansible_host: 192.168.56.10
    #       ansible_user: vagrant
    #       ansible_ssh_private_key_file: ~/.vagrant.d/insecure_private_key
