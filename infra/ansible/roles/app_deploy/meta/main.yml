---
galaxy_info:
  author: vamsi2ganagalla
  description: Render K8s manifests with vault-decrypted secrets and apply to local minikube cluster
  company: CSE 816 academic project
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags:
    - kubernetes
    - deploy

dependencies:
  - role: common
  - role: docker
  - role: kubernetes
