server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fintech_auth
    username: fintech_user
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true